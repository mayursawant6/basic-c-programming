# include<stdio.h>
int main() {
    int age;
    printf("Enter Age ");
    scanf("%d",&age);  // scan function=input function,there should be no space between % and d
    printf("age is: %d",age);
    return 0;
}